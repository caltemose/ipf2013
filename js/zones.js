Zepto(function($){
	
	// 1. figure out hash (zone number)
	var hash = window.location.hash;
	if (hash === "") hash = '#1';
	hash = hash.replace('#', '');
	
	// 2. determine positioning/css for this zone
	var mapCoords = [300,300];
	var zoneCoords = [300,300];
	
	$('#zone').css('background-image', '
	
	// 3. center the damn map
	
});